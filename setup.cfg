# Ship a universal wheel so Python 2.7 consumers can install from the wheel
# rather than falling through to the sdist, where setuptools 44.1.1 cannot read
# PEP 621 metadata and produces a metadata-less UNKNOWN-0.0.0 install.
[bdist_wheel]
universal = 1
