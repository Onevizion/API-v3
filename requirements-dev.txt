# Development dependencies for testing across Python 2.6 - 3.13
#
# For Python 2.6-2.7:
#   pip install "pytest<5.0" "pytest-cov<2.6" "vcrpy<2.0"
#
# For Python 3.4+:
#   pip install pytest pytest-cov vcrpy
#
# Note: This file uses environment markers for compatibility

# Testing framework
# pytest 4.6.x is the last version supporting Python 2.7
# pytest 6.x dropped Python 3.5 support
pytest>=3.6,<5.0; python_version < '3.0'
pytest>=6.2; python_version >= '3.6' and python_version < '3.10'
pytest>=7.0; python_version >= '3.10'

# Coverage
pytest-cov>=2.5,<2.6; python_version < '3.0'
pytest-cov>=3.0; python_version >= '3.6'

# HTTP mocking with VCR
vcrpy>=1.13,<2.0; python_version < '3.0'
vcrpy>=4.0; python_version >= '3.6'

# For Python 2.6/2.7 compatibility
mock; python_version < '3.0'

# Linting (Python 3.6+ only)
flake8>=3.8; python_version >= '3.6'
black>=22.0; python_version >= '3.7'

# Type checking (Python 3.7+ only)
mypy>=0.900; python_version >= '3.7'
types-requests; python_version >= '3.7'
