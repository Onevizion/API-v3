# Example environment variables for integration tests
# Copy this to .env and fill in your credentials

# OneVizion instance for integration tests
OV_URL=https://fiberlab.onevizion.com
OV_USERNAME=your_username
OV_PASSWORD=your_password

# Optional: Set to "true" to run integration tests
RUN_INTEGRATION_TESTS=false
